/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NhItem } from '../../models/nh-item';

export interface NhItemsGet$Params {

/**
 * Your UUID secret key, granted to you by the Nookipedia team. Required for accessing the API.
 */
  'X-API-KEY': string;

/**
 * The version of the API you are calling, written as `1.0.0`. This is specified as required as good practice, but it is not actually enforced by the API. If you do not specify a version, you will be served the latest version, which may eventually result in breaking changes.
 */
  'Accept-Version': string;

/**
 * When set to `true`, only item names are returned. Instead of an array of objects with all details, the return will be an array of strings.
 */
  excludedetails?: string;
}

export function nhItemsGet(http: HttpClient, rootUrl: string, params: NhItemsGet$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<NhItem>>> {
  const rb = new RequestBuilder(rootUrl, nhItemsGet.PATH, 'get');
  if (params) {
    rb.header('X-API-KEY', params['X-API-KEY'], {});
    rb.header('Accept-Version', params['Accept-Version'], {});
    rb.query('excludedetails', params.excludedetails, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<NhItem>>;
    })
  );
}

nhItemsGet.PATH = '/nh/items';
