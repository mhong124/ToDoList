/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { NhClothing } from '../../models/nh-clothing';

export interface NhClothingClothingGet$Params {

/**
 * The name of the clothing you wish to retrieve information about.
 */
  clothing: string;

/**
 * Your UUID secret key, granted to you by the Nookipedia team. Required for accessing the API.
 */
  'X-API-KEY': string;

/**
 * The version of the API you are calling, written as `1.0.0`. This is specified as required as good practice, but it is not actually enforced by the API. If you do not specify a version, you will be served the latest version, which may eventually result in breaking changes.
 */
  'Accept-Version': string;

/**
 * Specify the desired width of returned image URLs. When unspecified, the linked image(s) returned by the API will be full-resolution. Note that images can only be reduced in size; specifying a width greater than than the maximum size will return the default full-size image URL.
 */
  thumbsize?: number;
}

export function nhClothingClothingGet(http: HttpClient, rootUrl: string, params: NhClothingClothingGet$Params, context?: HttpContext): Observable<StrictHttpResponse<NhClothing>> {
  const rb = new RequestBuilder(rootUrl, nhClothingClothingGet.PATH, 'get');
  if (params) {
    rb.path('clothing', params.clothing, {});
    rb.header('X-API-KEY', params['X-API-KEY'], {});
    rb.header('Accept-Version', params['Accept-Version'], {});
    rb.query('thumbsize', params.thumbsize, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<NhClothing>;
    })
  );
}

nhClothingClothingGet.PATH = '/nh/clothing/{clothing}';
