import { Component, signal } from '@angular/core';
import {RouterLink} from '@angular/router';

@Component({
  selector: 'nav-custom',
  imports: [RouterLink],
  templateUrl: './nav.html',
  styles : ""
})
export class Nav {
  
}
